# 云原生 RAG 架构设计准则

## 1. 为什么向量检索推荐 HNSW 索引
在工业级大模型外挂知识库场景中，HNSW（分层可导航小世界图）算法相比传统的倒排文件（IVF_FLAT）具有极高的查询召回率。
即使在大规模向量聚集下，HNSW 依然能在毫秒级收敛至最佳近邻点。

## 2. 余弦距离（Cosine Similarity）与内积（IP）
针对 Transformer 架构提取的文本嵌入（如 nomic-embed-text），由于向量均已经过 L2 范数归一化，余弦距离等于向量点积。
余弦相似度取值在 -1 到 1 之间，非常适合设置绝对截断阈值来过滤幻觉。
